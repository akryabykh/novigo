# TODO — что осталось, вопросы, баги

## 🔴 Сразу (deploy)
- [ ] **Влить `dev` → `main`** (PR/merge в GitHub Desktop) — в `dev` висит фикс гейта `4d66dcb`
      (логин в аккаунт без профиля больше не застревает на `/login`). Без merge — не в проде.
- [ ] После деплоя: на `novigo-orpin.vercel.app` сделать `Cmd+Shift+R` и проверить вход своим
      аккаунтом (кэш держал старый сломанный бандл).

## 🟡 Инфраструктура
- [ ] **dev-preview недоступен** (`novigo-git-dev-…vercel.app` отдавал 000). Разобраться: билд упал
      / protection / протухший алиас. Пока тестировать на проде. (Проверить Vercel → Deployments →
      статус dev-деплоя, Build Logs.)
- [ ] **Supabase cold start**: free tier «засыпает» → первый запрос ~3с. Не баг, но UX страдает.
      Варианты: keep-warm пинг (cron), либо платный план. Обсудить с пользователем.
- [ ] Vercel: env-переменные вшиваются при сборке → после их изменения нужен Redeploy без кэша.

## 🟢 Продукт / UX (по фидбэку и открытым вопросам)
- [ ] **Дизайн-полиш** — пользователь чувствителен к визуалу; продолжать причёсывать (главная,
      плашки, кольца). Держать чистый минимал.
- [ ] Возможно упростить **ввод весов** (пользователь думал: авто-равные / приоритет вместо %).
      Пока НЕ трогали — уточнить, если вернётся.
- [ ] «Подсказки» — пользователь просил переработать; сделали текст формулы (60/40, 80/20) в
      онбординге. Возможно захочет «?»-пояснения прямо на главном/в форме.
- Открытые вопросы (из `~/Downloads/Novigo_overview.md`, для внешней оценки):
  - Понятна ли формула колец обычному пользователю (не слишком «математично»)?
  - Ок ли, что неделя/месяц не дойдут до 100% без целей на всех уровнях (фикс. доли)?
  - Что добавить для мотивации/удержания на горизонте 28 дней?
  - Одна сессия за раз — правильно, или нужны параллельные?

## ⚪️ Технический долг / потом
- [ ] iOS/Android сборки через EAS (код готов, не собирали).
- [ ] `daily_logs`-запросы бьются по `.in(...)` — при росте данных подумать про пагинацию/оптимизацию.
- [ ] Достижения/heatmap считаются по активной сессии — при новой сессии XP «стартует заново»
      (осознанное упрощение MVP; если надо — агрегировать по всем сессиям).
- [ ] Сброс пароля (`forgot-password`) шлёт письмо-ссылку; без своего SMTP и Site URL работает
      криво — допилить, если понадобится (задать Site URL = прод-домен в Supabase Auth).

## ✅ Уже сделано (не переделывать)
- Модель «сессия целей», кольца 60/40 & 80/20, инлайн +/−, онбординг, редактирование/удаление
  сессии с подтверждением, админ-режим со скруббером, no-op lock (дедлок), перенос роута админа,
  фикс повторного экрана имени при регистрации, таймауты на auth, логаут-кнопка (Pressable+hitSlop).
